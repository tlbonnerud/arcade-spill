# ui/

Alt som er skjerm og ikke spill:

- `hud.gd` — poeng, liv, bølge, aktive oppgraderinger (flyttes ut av main.gd).
- `transitions.gd` — bølgebanner, "ADVARSEL", fade, VICTORY/GAME OVER.

Kommer i roadmap steg 1.
